module.exports = {
    HTTP_PORT: process.env.HTTP_PORT || 3001,
    NODE_PORT: process.env.NODE_PORT || 5001,
    DIFFICULTY: 5,
    MINE_RATE: 1000 * 60,
    INITIAL_BALANCE: 500,
    MINING_REWARD: 50
}